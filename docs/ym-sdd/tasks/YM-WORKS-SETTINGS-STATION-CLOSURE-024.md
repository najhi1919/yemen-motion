# YM-WORKS-SETTINGS-STATION-CLOSURE-024

## الحالة

مكتملة ومغلقة بتاريخ 2026-07-27.

## المحطة

- الاسم: إعدادات وصلاحيات الأعمال.
- المسار المعتمد: `/admin/works/settings`.
- المساحات:
  1. الإعدادات التشغيلية.
  2. سير العمل.
  3. الوصول الحالي.
  4. كتالوج الصلاحيات.

## عقد الإعدادات والحفظ

- تشمل الإعدادات مهلة المراجعة وثقة النشر المباشر وحدود وأنواع الوسائط.
- يستخدم التحميل `GET /api/admin/works/settings`.
- يستخدم الحفظ الجزئي `PATCH /api/admin/works/settings`.
- يرسل الحفظ الحقول المتغيرة والمسموح بها فقط مع `version` الحالي.
- بقيت معالجة `401` و`403` و`409` و`422` و`500` معتمدة.
- يحتفظ تعارض `409` بالمسودة المحلية حتى يختار المستخدم إعادة التحميل.
- ترتبط حالة `Dirty` بالمقارنة مع النسخة المحملة ولا تتأثر بأدوات المستكشف.
- يحمي حارس المسار و`beforeunload` التغييرات غير المحفوظة.
- يتطلب تفعيل ثقة النشر المباشر تأكيدًا صريحًا؛ الإلغاء لا يرسل `PATCH`.
- لا يتطلب تعطيل ثقة النشر المباشر التأكيد الخاص.

## الوصول وكتالوج الصلاحيات

- يعرض نموذج الوصول قدرات الحساب الحالي، ولا يمثل محررًا للأدوار.
- الكتالوج مرجع للقراءة فقط ولا يغيّر الأدوار أوإسنادات المستخدمين.
- يحتوي الكتالوج على 79 صلاحية ضمن 11 مجموعة.
- يدعم البحث المحلي في التسمية والمفتاح والوصف واسم المجموعة.
- يدعم فلتر المجموعة وفلتر وجود الوصف وإعادة الضبط.
- تدعم المجموعات `Accordion` مستقلًا وفتح الكل وطي الكل.
- يعرض `Drawer` التفاصيل مع الإغلاق عبر `Escape` وحصر التركيز واستعادته.

## التصميم والاستجابة

- يستخدم القسم العلوي `AdminPageHero` المعتمد.
- يدعم `RTL` و`LTR`، و`Light` و`Dark`.
- يعمل على `Desktop` و`Tablet` و`Mobile` دون `Horizontal overflow`.
- تعرض روابط المساحات محتوى مساحة واحدة في كل مرة دون طلبات بيانات إضافية.
- أزيلت طبقات التنسيق المتراكمة واعتمد عقد بصري محلي موحد.
- أُغلق تسرب تنسيقات `Light mode` خارج نطاق صفحة الإعدادات.
- أصبح سطح `AppTopBar` شبه معتم في الوضع الفاتح دون تغيير `sticky` أو`z-index`.

## التحقق

- `WorksSettingsPersistenceTest.php`
- `WorksSettingsApiTest.php`
- `WorksSettingsMutationApiTest.php`
- الإجمالي: 77 اختبارًا / 468 تأكيدًا، وجميعها ناجحة.
- `npm run build`: ناجح للعميل والخادم و`Nitro`.
- فحص `CSS` المجمّع: لا `opacity` على جذر الوضع الفاتح ولا إعادة تعريف عامة لمتغيرات السمة.
- `git diff --check`: ناجح.
- التحقق الوظيفي: نجح للحفظ الجزئي والإلغاء والتأكيد والحراس وحالات الأخطاء.
- التحقق البصري: نجح في `Light` و`Dark` و`RTL` و`LTR` و`Tablet` و`Mobile`.
- `Console errors`: لا يوجد.
- طلبات `API` الفاشلة أوالإضافية غير المتوقعة: لا يوجد.

## التحذيرات المعروفة غير المانعة

- `sourcemap warning`.
- mixed static/dynamic `authStore` import.
- `chunk size warning`.

## حدود الإغلاق

- لا تغيير في `Backend` أو`API` أوالمسارات أوعقد الحفظ.
- لا تغيير في بيانات الصلاحيات أوعددها.
- لا وظائف جديدة ضمن الإغلاق.
