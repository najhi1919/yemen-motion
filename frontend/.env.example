# Yemen Motion frontend environment example
# Copy this file to frontend/.env when local frontend runtime config is needed.

# Public API base URL used by Nuxt runtimeConfig.public.apiBaseUrl.
# Local default matches frontend/nuxt.config.ts fallback.
NUXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api
